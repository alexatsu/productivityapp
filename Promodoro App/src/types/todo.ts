export type Todo = {
    id: string; 
    text: string,
    startTime: number,
    deadline: number, 
    completed: boolean
}

export type TodoForm = {
    todoText: string;
    todoStartTime: string; 
}

